#include "Player.h"


Player::Player()
{
	position[X] = 0.f;
	position[Y] = 0.f;
	//controller = CharacterController(this);
}

Player::~Player()
{
}
//
//void Player::update(float deltaTime)
//{
//	controller.update(deltaTime);
//}